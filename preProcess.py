from Hough_transform import fast_tracker

class preprocess:
	def __init__(self, image, radius, center)
	self.image = image
	self.radius = radius
	self.center = center

	setup = tracker(image, (16, 16), (40, 50), threshold, radius)
	fast_tracker.prepossing()